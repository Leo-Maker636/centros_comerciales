# Transformación Facturas: transformacion_fact
**Breve descripción del proyecto:** qué hace, para qué sirve, etc.
## Requisitos
> [!WARNING] Python 3.11+ 
## Instalación
Se recomienda usar un entorno virtual (.venv) para mantener las dependencias aisladas.
#### 1. Crear el entorno virtual
En la raíz del proyecto, ejecuta:
```bash
python -m venv .venv
```
Esto creará un entorno virtual llamado .venv.
#### 2. Activar el entorno virtual
En `Linux/macOS`:
```bash
source .venv/bin/activate
```
En `Windows (PowerShell)`:
```bash
.venv\Scripts\Activate.ps1
```
#### 3. Instalar las dependencias

```bash
pip install --upgrade pip
pip install build
pip install .
```
Esto instalará el proyecto y sus dependencias en tu .venv.
## Uso
```bash
nombre_del_comando
```
